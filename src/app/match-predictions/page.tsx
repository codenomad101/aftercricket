
import { getLiveMatches } from '@/lib/cricket-scraper';
import MatchCard from '@/components/MatchCard';

export const revalidate = 60; // Revalidate every minute

export default async function MatchPredictionsPage() {
    const matches = await getLiveMatches();

    // Use all matches from live scores as requested
    const activeMatches = matches;

    return (
        <div className="min-h-screen bg-gradient-to-b from-purple-50 via-white to-blue-50 py-8">
            <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                {/* Header */}
                <div className="text-center mb-12">
                    <h1 className="text-4xl md:text-5xl font-black text-transparent bg-clip-text bg-gradient-to-r from-purple-600 to-indigo-600 mb-4 font-papyrus">
                        AI Match Predictions
                    </h1>
                    <p className="text-lg text-gray-600 max-w-2xl mx-auto">
                        Get real-time AI-powered insights and win probability predictions for all major cricket matches.
                        Our advanced models analyze team form, venue stats, and match conditions.
                    </p>
                </div>

                {/* Content */}
                {activeMatches.length > 0 ? (
                    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                        {activeMatches.map((match) => (
                            <MatchCard key={match.id} match={match} showPrediction={true} />
                        ))}
                    </div>
                ) : (
                    <div className="text-center py-12 bg-white rounded-3xl shadow-lg">
                        <div className="text-6xl mb-4">🏏</div>
                        <h3 className="text-2xl font-bold text-gray-800 mb-2">No Active Matches</h3>
                        <p className="text-gray-500">
                            There are no live or upcoming matches available for prediction right now.
                            Please check back later!
                        </p>
                    </div>
                )}

                {/* Disclaimer */}
                <div className="mt-16 p-6 bg-yellow-50 rounded-xl border border-yellow-100 text-center">
                    <p className="text-sm text-yellow-800">
                        <strong>Disclaimer:</strong> These predictions are generated by AI models for entertainment purposes only.
                        Cricket is a game of uncertainties, and actual results may vary. Please do not use these for betting.
                    </p>
                </div>
            </div>
        </div>
    );
}
